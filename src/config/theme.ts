/** HTML attribute name that drives the active colour theme. */
export const THEME_ATTRIBUTE = 'data-theme';

/** Theme applied when no explicit preference is stored or detected. */
export const DEFAULT_THEME = 'dark';

/** CSS media query string for the OS-level reduced-motion preference. */
export const PREFERS_REDUCED_MOTION_QUERY = '(prefers-reduced-motion: reduce)';
